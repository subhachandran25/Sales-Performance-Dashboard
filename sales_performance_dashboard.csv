import pandas as pd
import random
from faker import Faker

fake = Faker()
regions = ['North', 'South', 'East', 'West']
data = []

for region in regions:
    auh_name = fake.name()
    for sm_idx in range(1, 4):
        sm_name = fake.name()
        for mgr_idx in range(1, 6):
            mgr_name = fake.name()
            for rep_idx in range(1, 11):
                rep_name = fake.name()
                
                new_leads = random.randint(50, 200)
                disqualified = random.randint(5, 40)
                no_answer = random.randint(10, 60)
                qualified = max(0, new_leads - disqualified - no_answer)
                converted = random.randint(0, qualified)
                deals_closed = random.randint(0, converted)
                
                total_revenue = deals_closed * random.randint(500, 5000)
                avg_unit_value = (total_revenue / deals_closed) if deals_closed > 0 else 0
                
                data.append([
                    region, auh_name, sm_name, mgr_name, rep_name,
                    random.randint(100, 1000), random.randint(100, 2000),
                    new_leads, disqualified, no_answer, qualified,
                    converted, deals_closed, random.randint(5, 50),
                    total_revenue, round(avg_unit_value, 2)
                ])

columns = ['Region', 'AUH_Name', 'Senior_Manager_Name', 'Sales_Manager_Name', 'Sales_Rep_Name', 
           'Calls_Dialed', 'Call_Time_Mins', 'New_Leads', 'Disqualified', 
           'No_Answer', 'Qualified', 'Converted', 'Deals_Closed', 'Followup_Leads',
           'Total_Revenue', 'Avg_Unit_Value']

df = pd.DataFrame(data, columns=columns)
df.to_csv('sales_performance_data.csv', index=False)
print("CSV Generated Successfully.")
